{
  "name": "healthcare-record-system-root",
  "version": "1.0.0",
  "description": "Root package for running both frontend and backend",
  "scripts": {
    "dev": "npx concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  }
}
